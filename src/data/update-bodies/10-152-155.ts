export const body = "窗口末端。`0.154.0` 是命令与参数最大的一跳：托管 worktree 进根参数，顶层 `mcp-server` 退场。`0.152`–`0.153.x` 多数是 TUI / 模型目录补丁。\n\n## 命令与参数\n\n`0.152.0`–`0.153.4`：顶层命令集合不变（仍含 `mcp-server`）。特性表继续涨（sleep_tool 回来、`context_management` 出现），但 `--help` 动词没动。\n\n`0.153.4 → 0.154.0`：\n\n- **去掉顶层 `mcp-server`**。`codex mcp` 仍然只管理外部服务器（`list` / `get` / `add` / `remove` / `login` / `logout`）。\n- 根命令以及 `exec` / `fork` / `resume` / `queue` / `archive` / `delete` 增加 `--worktree`。\n- `exec-server` 增加 AWS SigV4 相关参数和 `--remote-transport`。\n\n`0.155.0`：特性表增加 `api_key_model_discovery` 等；`remote_compaction_v2` 从 stable 标成 removed。`0.155.1` 命令与参数无变化。\n\n## 主要功能变化\n\n- **`0.152.0`**：Vim 草稿内 `/`、`?` 搜索。MCP 服务器名允许 `: @ / .`，才能写包名风格的 id。单个 MCP 工具可设 `output_token_limit`。规划工具 `update_plan` 默认关，要显式 `tools.update_plan.enabled = true`。\n- **`0.153.0`**：Vim `u` / `Ctrl+R` 撤销重做。插件 CLI 可对远程 marketplace 列表/安装/卸载。`tui.auto_recap = false` 只关自动 recap，手动 `/recap` 还在。\n- **`0.153.1`–`0.153.4`**：GPT-6-Astra 进 API / Bedrock / 捆绑目录；多数是模型元数据，不是新子命令。\n- **`0.154.0`**：实验性托管 worktree：`--worktree` 或 `/worktree`。可在 Codex 继续干活时内联回答问题。Windows 可共享后台 Codex 服务。\n- **`0.155.0`**：实验性 `/voice`（经 `/experimental`）。Agents 总览可隐藏/归档/删除任务。macOS 本地 TUI 上 MCP 请求可走 Touch ID。\n- **`0.155.1`**：新本地 TUI 会话默认**不再**开 reasoning summaries，避免不支持该字段的供应商拒请求。显式配置仍有效。\n\n## 升级时\n\n- 将 Codex 作为 MCP 服务器供其他客户端使用：不要再抄 `codex mcp-server`。对照当前 `codex help` 和官方 MCP 指南。\n- 并行任务优先试 `codex --worktree`，本机没有入口就先 `/experimental`，不要抄未进你那一版的参数。\n- MCP 配置里的服务器名可以从 `0.152` 起使用 `shopify@openai-curated` 这种包名，可以保留 `@`。\n- 自动 recap 干扰上下文时，只关 `tui.auto_recap`，不要误关整个实验上下文管理。\n\n这一段发行说明很密，官方 [Learn changelog](https://learn.chatgpt.com/docs/changelog) 从 0.146 起也开始按版本折叠。命令是否存在，仍以本机 `codex --help` 为准。\n";
