export const body = "## 0.155.1\n\n- 变更：新建本地 TUI 会话默认关闭 reasoning summaries，减少不支持该字段的模型服务返回的请求错误。显式配置仍然有效。\n\n## 0.155.0\n\n- 新增：实验性 `/voice`，可通过 `/experimental` 启用。\n- 新增：Agents 总览支持隐藏、归档和删除任务。\n- 新增：macOS 本地 TUI 的 MCP 请求支持 Touch ID。\n- 新增：`api_key_model_discovery` 等特性键。\n- 变更：`remote_compaction_v2` 从 stable 标记为 removed。\n\n## 0.154.0\n\n- 新增：实验性托管工作树，可通过 `--worktree` 或 `/worktree` 使用。\n- 新增：`exec`、`fork`、`resume`、`queue`、`archive` 和 `delete` 支持 `--worktree`。\n- 新增：任务继续运行时可内联回答问题。\n- 改进：Windows 支持共享后台 Codex 服务。\n- 新增：`exec-server` 的 AWS SigV4 相关参数和 `--remote-transport`。\n- 移除：顶层 `codex mcp-server`。`codex mcp` 继续用于管理外部服务器，保留 `list`、`get`、`add`、`remove`、`login` 和 `logout`。\n\n## 0.153.1–0.153.4\n\n- 新增：API、Bedrock 及捆绑模型目录中的 GPT-6-Astra 支持。\n\n## 0.153.0\n\n- 新增：Vim 编辑支持 `u` 撤销和 `Ctrl+R` 重做。\n- 新增：插件 CLI 支持远程 marketplace 的列表、安装和卸载。\n- 新增：`tui.auto_recap = false`，关闭自动 recap；手动 `/recap` 仍可使用。\n\n## 0.152.0\n\n- 新增：Vim 草稿内通过 `/` 和 `?` 搜索。\n- 改进：MCP 服务名称支持 `:`、`@`、`/`、`.`，可使用包名形式的标识。\n- 新增：单个 MCP 工具可配置 `output_token_limit`。\n- 变更：规划工具 `update_plan` 默认关闭，可通过 `tools.update_plan.enabled = true` 启用。\n";
