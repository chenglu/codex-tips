export const body = "这一段 `debug` 独立成命令，`exec` 加上 `--ephemeral`，Skills 的主目录换成 `.agents/skills`。Plan / Steer / personality 陆续变成默认行为。\n\n## 命令面\n\n`0.93.0`：`features` 增加 `enable` / `disable`，不必只靠手改 `config.toml`。\n\n`0.95.0`：顶层出现 `debug`，当时子命令是 `debug app-server`。更早版本里 `sandbox` 的帮助还写着 `[aliases: debug]`。\n\n`0.99.0`：`exec`、`exec resume`、`exec review` 增加 `--ephemeral`（一次性线程，不进会话列表）。\n\n`0.100.0`：`app-server` 增加 `--listen`。\n\n`features list` 开始出现 `stable` / `deprecated` / `removed` 三态，不再只有 experimental。`collaboration_modes`、`personality`、`unified_exec`、`steer`、`shell_snapshot` 在这一段先后标成 stable。`web_search_cached` 很快被标 deprecated。`0.100.0` 把 Windows elevated sandbox 相关键标成 removed。\n\n## 这一段真正改变用法的点\n\n- **`0.92.0`**：MCP 可在 `config.toml` 写 OAuth scopes；缓存版 `web_search` 一度成为客户端默认。多代理加了 explorer 角色和 max-depth。\n- **`0.93.0`**：Plan 模式把提案流到独立 TUI 视图；`/plan` 有特性开关。`/apps` 浏览连接器。智能审批默认开，MCP 工具调用会显式要批准。\n- **`0.94.0`**：Plan 默认开。`personality` 稳定，默认 friendly。技能从 `.agents/skills` 加载。\n- **`0.95.0`**：macOS 上 `codex app PATH` 可拉起桌面应用。个人技能读 `~/.agents/skills`（兼容 `~/.codex/skills`）。shell 工具拿到 `CODEX_THREAD_ID`。\n- **`0.97.0`**：MCP/App 审批可「允许并记住」（会话级）。技能文件改动可热更新。TUI 增加 `/debug-config`。\n- **`0.98.0`**：GPT-5.3-Codex。Steer 默认开：任务跑着时 `Enter` 直接发送，`Tab` 才排队。\n- **`0.99.0`**：前台 `!` 命令不再打断当前回合。`/statusline` 配置页脚。企业 `requirements.toml` 可限制 web search 和网络。\n- **`0.100.0`**：实验性 `js_repl`；Linux/Windows 沙箱能力上调。\n\n## 升级时\n\n把仓库技能从 `.codex/skills` 迁到 `.agents/skills`，个人技能放到 `~/.agents/skills`。CI 里一次性审查用 `codex exec --ephemeral`。不要再把 `debug` 当成 `codex sandbox` 的别名。`features enable` / `disable` 从 `0.93` 起才存在。\n";
