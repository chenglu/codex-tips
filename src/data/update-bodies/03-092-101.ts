export const body = "## 0.100.0\n\n- 新增：`app-server --listen`。\n- 新增：实验性 `js_repl`。\n- 改进：Linux 与 Windows 沙箱能力。\n- 变更：Windows elevated sandbox 相关特性键标记为 removed。\n\n## 0.99.0\n\n- 新增：`exec`、`exec resume` 和 `exec review` 支持 `--ephemeral`，用于不保存到会话列表的一次性任务。\n- 改进：前台 `!` 命令不再打断当前回合。\n- 新增：通过 `/statusline` 配置页脚。\n- 新增：企业 `requirements.toml` 可限制网页搜索和网络访问。\n\n## 0.98.0\n\n- 新增：GPT-5.3-Codex 支持。\n- 变更：Steer 默认启用。任务运行时，`Enter` 直接发送消息，`Tab` 将消息加入队列。\n\n## 0.97.0\n\n- 新增：MCP 和 App 工具审批支持在当前会话内“允许并记住”。\n- 改进：技能文件修改后可热更新。\n- 新增：TUI `/debug-config`。\n\n## 0.95.0\n\n- 变更：`debug` 成为独立命令，提供 `debug app-server`，不再作为 `sandbox` 的别名。\n- 新增：macOS 可通过 `codex app PATH` 打开桌面应用。\n- 变更：个人技能从 `~/.agents/skills` 加载，同时兼容 `~/.codex/skills`。\n- 新增：shell 工具环境中提供 `CODEX_THREAD_ID`。\n\n## 0.94.0\n\n- 变更：Plan 默认启用。\n- 变更：`personality` 标记为 stable，默认值为 friendly。\n- 变更：项目技能从 `.agents/skills` 加载。\n\n## 0.93.0\n\n- 新增：`features enable` 和 `features disable`。\n- 改进：Plan 提案显示在独立 TUI 视图中，`/plan` 由特性开关控制。\n- 新增：通过 `/apps` 浏览连接器。\n- 变更：智能审批默认启用；MCP 工具调用显式请求批准。\n\n## 0.92.0\n\n- 新增：在 `config.toml` 中配置 MCP OAuth scopes。\n- 变更：缓存版 `web_search` 成为客户端默认。\n- 新增：多代理 explorer 角色和最大深度设置。\n";
