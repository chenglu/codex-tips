export const body = "这一段有 10 个版本号，但 CLI 表面几乎只在 `0.143.0` 动过。其余是安装、Guardian、模型元数据和安全补丁。\n\n## 命令与参数\n\n`0.142.4`、`0.142.5`、以及 `0.144.1`–`0.144.6`：实测 `--help` / `features list` 相对前一版没有命令或参数增减。\n\n`0.142.5 → 0.143.0`：\n\n- `remote-control` 增加子命令 `pair`（`--json`）\n- `sandbox` 把 `--permissions-profile` **改名为** `--permission-profile`（少一个 s）\n- 同时增加 `--sandbox-state-json`、`--sandbox-state-disable-network`、`--sandbox-state-readable-root`\n- `remote_plugin` 标 stable；`code_mode_host` 进入特性表\n\n`0.143.0 → 0.144.0`：`imagegenext` 从特性表拿掉；`auth_elicitation` 和 `code_mode_host` 标 stable。\n\n## 补丁版实际修了什么\n\n发行说明里面向用户的点，浓缩如下：\n\n- **`0.142.5`**：避免把完整 Responses WebSocket 请求打进 trace 日志。\n- **`0.143.0`**：远程插件默认开；可走系统代理（含 PAC/WPAD）；Bedrock 上的 GPT-5.6 Sol / Terra / Luna；MCP tool search 默认开。\n- **`0.144.0`**：用量重置额度可选哪一张；MCP 工具可交互式要登录，不必再开实验开关；`writes` 这种 app 审批模式。\n- **`0.144.1`**：修复独立安装器在 GitHub 元数据压缩/乱序时失败；保证 code-mode host 二进制被装上。\n- **`0.144.2`**：回滚一次 Guardian 自动审批提示回归。\n- **`0.144.5`**：收紧危险命令检测（包括更多 `rm` 形式）。\n- **`0.144.6`**：修正 GPT-5.6 系列捆绑说明和 272k 上下文窗口。\n\n`0.144.3` / `0.144.4` 发行说明写的是无用户可见变更。\n\n## 升级时\n\n脚本里的 `codex sandbox --permissions-profile` 在 `0.143` 起会过时，改 `--permission-profile`。远程守护进程配对用 `codex remote-control pair`，不要手写过期的 daemon 参数。若只是为了模型目录或安装器，跳到最新的 `0.144.x` 即可，不必在每个补丁号上停留。\n";
