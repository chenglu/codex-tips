export const body = "这一段顶层命令一个没加，特性表从 32 项涨到 46 项。插件、memories、Guardian 审批和运行时要权限，都先以 feature 的形式出现。\n\n## 命令面\n\n仍是 `0.95` 那 15 个命令：`exec` … `debug` … `features`。\n\n值得记的旗标：\n\n- `0.105.0`：`exec` 增加 `--progress-cursor`；`exec resume` / `exec review` 增加 `--output-last-message`。\n- `features list` 新增（随后有的又改阶段或删掉）：`multi_agent`、`memories`、`plugins`、`fast_mode`、`guardian_approval`、`request_permissions_tool`、`image_generation`、`voice_transcription`、`js_repl`。\n\n`0.102.0` 去掉 `collab` 这个旧名，改走 `multi_agent`。`0.105.0` 用 `memories` 替换 `memory_tool`。`0.106.0` 去掉 `skill_approval`，并把已经 stable 的 `steer` 标成 removed——Steer 的交互还在，只是不再当可关的 feature 键。`0.113.0` 把 `sqlite` 日志库标成 removed。\n\n## 这一段真正改变用法的点\n\n- **`0.102.0`**：更统一的权限流；网络审批会显示 host/协议；多代理角色可配置。\n- **`0.105.0`**：TUI 高亮代码块和 diff，`/theme` 可实时预览。按住空格语音输入（`features.voice_transcription = true`，当时仍 under development）。`/copy`、`/clear`、`Ctrl-L`。子代理可从 CSV 扇出。\n- **`0.106.0`**：官方安装脚本进 GitHub Release；`js_repl` 推进 `/experimental`；默认协作模式也能 `request_user_input`。\n- **`0.107.0`**：可把当前线程 fork 成子代理。`codex debug clear-memories` 重置记忆。\n- **`0.110.0`**：插件系统（技能 + MCP + App，可走本地 marketplace）。TUI `/fast`。Windows 安装脚本进发布物。\n- **`0.111.0`**：Fast 模式默认开。会话开始时会告诉模型已启用哪些插件。\n- **`0.112.0`**：聊天里 `@plugin` 可点名插件。\n- **`0.113.0`**：内置 `request_permissions` 工具，运行中的回合能再要权限。精选 marketplace、更完整的 `plugin/list`。图片生成写到当前工作目录。\n\n二进制从约 88 MB 跳到 128 MB，主要是资源和运行时，不是多了一两个子命令。\n\n## 升级时\n\n不要把 `collab`、`memory_tool`、`steer` 这些已经从 `features list` 消失的键写进 `config.toml`。新团队配置用 `multi_agent`、`memories`、`plugins`、`fast_mode`，并以本机 `codex features list` 的阶段为准。语音输入默认没开。Guardian / `request_permissions` 会让审批弹窗变多，这是预期。\n";
