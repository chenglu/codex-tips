export const body = "会话管理从「只能继续或分叉」变成「可以归档、恢复、永久删除」。插件命令补上机器可读输出。`/import` 开始承接从 Claude Code 迁过来的配置。\n\n## 命令与参数\n\n| 版本 | 变化 |\n| --- | --- |\n| `0.136.0` | 增加 `archive`、`unarchive`。`app-server` 增加 `--stdio` |\n| `0.137.0` | `plugin list` 增加 `--available`、`--json` |\n| `0.138.0` | `plugin add` / `plugin remove` 增加 `--json` |\n| `0.140.0` | 增加 `delete`（带 `--force`）。`mentions_v2` 标 stable |\n\n这些会话命令的 `--help` 会带上一长串和根命令相同的会话参数（`--cd`、`--sandbox`、`--remote` 等）。真正要记的是语义：归档后的会话不能 resume/fork，除非 `unarchive`；`delete` 是永久删除。\n\n`0.141.0` 增加 `sleep_tool`。`0.142.0` 一批特性进表：`auto_compaction`、`rollout_budget`、`multi_agent_mode` 等。`0.142.1` / `0.142.2` 补系统代理和 MCP tool search 默认行为。`0.142.3` 发行说明写明没有面向用户的变化。\n\n## 主要功能变化\n\n- **`0.136.0`**：TUI `/archive` 与 CLI `codex archive` / `unarchive`。远程控制 WebSocket 改用短时服务端 token，不再塞 ChatGPT access token。\n- **`0.137.0`**：企业侧可见月度额度；远程控制可配对、列出或吊销 controller。\n- **`0.138.0`**：`/app` 可把当前 CLI 线程交给桌面应用（macOS 和原生 Windows）。\n- **`0.140.0`**：`/usage`。永久删除走 `codex delete` / `/delete`。**`/import` 可从 Claude Code 选迁设置、项目配置和近期聊天。** `@` 默认打开统一提及菜单。\n- **`0.141.0`**：远程执行走加密中继。TUI 输入提示在无操作一段时间后可自动确认。\n- **`0.142.0`**：`/plugins` 分成 OpenAI Curated / Workspace / Shared with me。可配置 rollout token 预算。出现索引式 web search 模式。\n\n二进制在 `0.142.3` 到过约 286 MB，是这 100 个版本里的高位之一。\n\n## 升级时\n\n团队如果还用「把 `sessions/` 目录改名」来藏旧会话，从 `0.136` 起应改用 `archive`。真正不要的会话才 `delete`。从 Claude Code 迁过来后仍要核对 `AGENTS.md` 和技能路径。自动化装插件请走 `codex plugin list --json`，使用结构化输出。\n";
