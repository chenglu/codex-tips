export const body = "对照范围是 npm 上最近 **100 个稳定版** `@openai/codex`：从 `0.69.0`（2025-12-10）到 `0.155.1`（2026-09-18）。版本号必须长得像 `0.155.1`，不含 `alpha`，也不含 `linux-x64` 这种平台后缀。\n\n窗口里没有 `0.70`、`0.82`、`0.83`、`0.108`、`0.109`、`0.126`、`0.127`。它们不是被我们跳过，而是这段时间里根本没有对应的稳定 npm 包。\n\n## 怎么跑的\n\n每个版本都是真实执行，不是只读 changelog：\n\n1. 有平台包就装 `@openai/codex@<version>-linux-x64`（从 `0.99.0` 起）；更早的版本用当时那份打进所有平台二进制的大包。\n2. 只抽出 `x86_64-unknown-linux-musl` 的 `codex` 可执行文件。\n3. 在隔离的 `CODEX_HOME` 里跑 `codex --version`、`codex --help`，再对每个子命令及其下一层跑 `--help`。\n4. 若存在 `features list`，就把特性名、阶段和默认开关记下来。\n\n100 个版本全部跑通，没有失败。68 个版本相对前一版改了命令、旗标或特性表；其余是发行说明有内容、但 `--help` 表面没动的补丁。\n\n本机没有登录、没有对仓库跑 agent。对照的是**命令面**，不是模型质量。\n\n## 九个月里命令表怎么长\n\n`0.69.0` 的顶层命令是 13 个：`exec`、`review`、`login`、`logout`、`mcp`、`mcp-server`、`app-server`、`completion`、`sandbox`、`apply`、`resume`、`cloud`、`features`。\n\n到 `0.155.1` 变成 25 个。增减都发生在这些点：\n\n| 版本 | 顶层变化 |\n| --- | --- |\n| `0.81.0` | 增加 `fork` |\n| `0.95.0` | 增加 `debug`（不再只是 `sandbox` 的别名） |\n| `0.119.0` | 增加 `exec-server` |\n| `0.121.0` | 增加 `marketplace` |\n| `0.122.0` | `marketplace` 改名为 `plugin` |\n| `0.128.0` | 增加 `update`；根命令去掉 `--full-auto` |\n| `0.130.0` | 增加 `remote-control` |\n| `0.131.0` | 增加 `doctor` |\n| `0.136.0` | 增加 `archive`、`unarchive` |\n| `0.140.0` | 增加 `delete` |\n| `0.148.0` | 增加 `migrate-rollouts`；`exec` 下出现 `fork` |\n| `0.149.0` | 增加 `agents`、`queue` |\n| `0.154.0` | 增加 `--worktree`；去掉顶层 `mcp-server` |\n\n二进制从约 54 MB 涨到约 269 MB。`features list` 从 16 项涨到 142 项，其中大量是 `under development` 或 `removed`。不要把这份表里的名字抄进团队 `config.toml`，除非本机 `codex features list` 显示 `stable`，并且你真的要开。\n\n## 升级时反复踩的坑\n\n- `--full-auto` 在 `0.128.0` 从 `--help` 消失。更早的脚本要改成明确的 `--sandbox` / `--ask-for-approval`，或 `dangerously-bypass-approvals-and-sandbox`。\n- `--profile-v2` 只在 `0.131.0`–`0.133.0` 出现，`0.134.0` 起又只认 `--profile`。\n- 审批策略 `untrusted` 在 `0.149.0` 离开 `--help`。现在根帮助只剩 `on-request` 和 `never`。\n- `codex mcp-server`（把 Codex 自身当 MCP 服务）在 `0.154.0` 从顶层消失。`codex mcp` 一直是管**外部** MCP 服务器。\n- 技能目录从 `.codex/skills` 迁到 `.agents/skills` / `~/.agents/skills`，大约从 `0.94`–`0.95` 开始成为主路径。\n\n## 怎么读后面 10 篇\n\n按时间顺序。每篇只写该区间里**命令面能核对**的变化，再用 GitHub `rust-v*` 发行说明补「TUI / 协议里发生了什么」。发行说明里的 PR 清单不整页抄。\n\n动手前仍以你机器上的 `codex --help`、`/help` 和 [官方 changelog](https://developers.openai.com/codex/changelog) 为准。\n";
