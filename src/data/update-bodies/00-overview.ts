export const body = "## 0.155.1\n\n- 新建本地 TUI 会话默认关闭 reasoning summaries，减少不支持该字段的模型服务返回的请求错误。显式配置仍然有效。\n\n## 0.155.0\n\n- 新增实验性 `/voice`，可在 `/experimental` 中启用。\n- Agents 总览支持隐藏、归档和删除任务。\n- macOS 本地 TUI 的 MCP 请求支持通过 Touch ID 验证。\n\n## 0.154.0\n\n- 新增实验性托管工作树，可通过 `--worktree` 或 `/worktree` 使用。\n- 支持在任务继续运行时内联回答问题。\n- 移除顶层 `codex mcp-server` 命令。`codex mcp` 继续用于管理外部 MCP 服务。\n\n## 0.152–0.153\n\n- MCP 服务名称支持 `:`、`@`、`/`、`.`，可使用包名形式的标识。\n- 单个 MCP 工具支持设置 `output_token_limit`。\n- 插件 CLI 支持远程 marketplace 的列表、安装和卸载。\n- TUI Vim 编辑支持搜索、撤销和重做。\n\n## 0.145–0.151\n\n- `0.147`：新增自动审批参数 `--approve-for-me`。\n- `0.148`：新增 `exec fork` 和 `migrate-rollouts`。\n- `0.149`：新增 `agents` 和 `queue`；审批策略 `untrusted` 不再出现在 `--help` 中。\n\n## 0.136–0.144\n\n- `0.136`：新增 `archive` 和 `unarchive`，支持归档及恢复会话。\n- `0.140`：新增 `delete`，支持永久删除会话；新增 `/import`，用于导入 Claude Code 配置。\n- `0.143`：新增 `remote-control pair`；将 sandbox 参数 `--permissions-profile` 更名为 `--permission-profile`。\n- `0.144.x`：修复安装器、Guardian 审批和模型元数据问题。\n\n## 0.124–0.135\n\n- `0.124`：Hooks 支持在 `config.toml` 和托管 `requirements.toml` 中配置。\n- `0.128`：新增 `codex update`；移除 `--full-auto`。\n- `0.129`：新增 TUI Vim 编辑模式 `/vim`，以及 `/hooks` 浏览和开关。\n- `0.130`：新增 `remote-control`。\n- `0.131`：新增 `doctor`，支持 JSON 和摘要输出。\n- `0.134`：移除临时参数 `--profile-v2`，保留 `--profile`。\n\n## 0.114–0.123\n\n- `0.119`：新增 `exec-server`。\n- `0.121`：新增 `marketplace` 命令，随后在 `0.122` 更名为 `plugin`。\n- 根命令新增 `--remote`；`plugins` 和 `multi_agent` 标记为 stable。\n\n## 0.102–0.113\n\n- 新增插件系统，支持组合技能、MCP 和 App。\n- 新增 memories、Guardian 审批及运行时权限请求相关能力。\n- TUI 新增 `/fast`、`/theme`、`/copy` 和 `/clear`；支持语音输入实验功能。\n- `0.113`：新增内置 `request_permissions` 工具。\n\n## 0.92–0.101\n\n- Skills 主目录迁移至 `.agents/skills`，个人技能使用 `~/.agents/skills`。\n- Plan、Steer、personality 和 unified_exec 陆续成为稳定或默认能力。\n- `0.95`：`debug` 成为独立命令。\n- `0.99`：`exec`、`exec resume` 和 `exec review` 新增 `--ephemeral`，支持不保存会话的一次性任务。\n\n## 0.69–0.91\n\n- 支持按会话加载 Skills。\n- `0.74`：新增 `/experimental`。\n- `0.78`：开始加载项目层 `.codex/config.toml`；支持通过 `Ctrl+G` 在外部编辑器中编写提示。\n- `0.79`：`exec resume` 和 `exec review` 支持模型、JSON 及 sandbox 等全局参数。\n- `0.81`：新增 `codex fork` 和 `--no-alt-screen`。\n";
