export const body = "## 0.135.0\n\n- 改进：`doctor` 提供更完整的诊断报告。\n- 新增：`/permissions` 支持命名权限 profile。\n\n## 0.134.0\n\n- 变更：移除 `--profile-v2`，使用 `--profile` 选择配置；旧写法会提示迁移。\n- 新增：`mcp add --oauth-client-id` 和 `--oauth-resource`。\n- 变更：sandbox 参数集中到父命令，简化平台子命令的帮助。\n- 新增：本地会话历史搜索。\n- 改进：声明 `readOnlyHint` 的只读 MCP 工具可并行执行。\n\n## 0.133.0\n\n- 变更：`plugin_hooks` 标记为 removed，插件钩子能力合并到正式 Hooks。\n- 新增：`remote-control --json`。\n- 变更：`exec-server --executor-id` 更名为 `--environment-id`。\n- 变更：`goals` 标记为 stable。\n\n## 0.132.0\n\n- 新增：`exec resume` 和 `exec review` 支持 `--output-schema`。\n\n## 0.131.0\n\n- 新增：`doctor`，支持 `--json`、`--summary` 和 `--all`，检查运行时、认证、终端、网络及配置。\n- 新增：`--strict-config`、`--dangerously-bypass-hook-trust` 和 `--profile-v2`。\n- 新增：`plugin add`、`list`、`remove` 和 `marketplace` 子命令。\n- 改进：`@` 提及支持搜索文件、插件和技能。\n- 变更：Python SDK 更名为 `openai-codex`；`plugin_hooks` 标记为 stable。\n\n## 0.130.0\n\n- 新增：`remote-control`，作为无头、可远程控制的 app-server 入口。\n\n## 0.129.0\n\n- 变更：登录参数 `--with-agent-identity` 更名为 `--with-access-token`。\n- 新增：`exec-server --name` 和 `--remote`。\n- 新增：TUI Vim 编辑模式 `/vim`。\n- 改进：重新设计 resume/fork 选择器。\n- 新增：通过 `/hooks` 浏览和启停钩子。\n\n## 0.128.0\n\n- 新增：`codex update` 自更新。\n- 移除：根命令、`exec`、`fork` 和 `resume` 的 `--full-auto`；脚本需显式配置沙箱与审批方式。\n- 新增：`login --with-agent-identity`。\n- 新增：TUI keymap 配置、`/goal` 工作流及外部 agent 会话导入。\n- 改进：权限 profile 提供内置默认值。\n- 变更：`js_repl` 标记为 removed，移除 `undo` 独立特性键。\n\n## 0.125.0\n\n- 新增：`app-server proxy --sock`，用于 Unix socket 连接。\n\n## 0.124.0\n\n- 变更：Hooks 标记为 stable，支持写入 `config.toml` 和托管 `requirements.toml`。\n- 新增：`hooks`、`codex_hooks`、`guardian_approval` 和 `browser_use` 等特性键。\n- 变更：符合条件的 ChatGPT 套餐默认使用 Fast 模式。\n- 新增：TUI 使用 `Alt+,` 和 `Alt+.` 调整推理力度。\n";
