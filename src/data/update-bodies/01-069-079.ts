export const body = "这一段顶层命令始终是 13 个，二进制大约 54 MB → 60 MB。变化主要在配置层、Skills 和 `exec` 子命令的旗标，不在新动词。\n\n## 命令面\n\n`codex --help` 从 `0.69.0` 到 `0.79.0` 仍是：`exec`、`review`、`login`、`logout`、`mcp`、`mcp-server`、`app-server`、`completion`、`sandbox`、`apply`、`resume`、`cloud`、`features`。\n\n`mcp` 已经能 `list` / `get` / `add` / `remove` / `login` / `logout`。`features` 当时只有 `list`，还不能 `enable` / `disable`。\n\n实测有 CLI 表面变化的版本：\n\n| 从 → 到 | 对照结果 |\n| --- | --- |\n| `0.71.0` → `0.72.0` | `features list` 增加 `enable_elevated_windows_sandbox` |\n| `0.73.0` → `0.74.0` | Windows sandbox 相关特性改名；`unified_exec` 进入 beta |\n| `0.76.0` → `0.77.0` | 去掉 `rmcp_client` 特性。发行说明也写了：streamable HTTP 的 MCP OAuth 不再依赖这颗开关 |\n| `0.78.0` → `0.79.0` | `exec resume` / `exec review` 终于接上模型、JSON、sandbox 那组全局旗标 |\n\n最后一条最值得改脚本。`0.79.0` 之前，`codex exec resume --json` 这类写法可能根本不在该子命令的 `--help` 里，即使根命令已经有同样的旗标。\n\n## 这一段真正改变用法的点\n\n- **`0.69.0`**：显式选中的 Skill 会把 `SKILL.md` 注入当前回合，技能按会话加载一次。\n- **`0.71.0` / `0.74.0`**：默认模型故事换成 GPT-5.2 和 `gpt-5.2-codex`。以本机 `/model` 为准。\n- **`0.74.0`**：TUI 增加 `/experimental`，用来试标成 experimental 的能力。\n- **`0.76.0`**：UNIX 上开始读 `/etc/codex/requirements.toml`；TUI 增加 `/ps`。\n- **`0.78.0`**：`Ctrl+G` 把当前提示送到 `$VISUAL` / `$EDITOR`。项目层 `.codex/config.toml` 开始按 `project_root_markers` 叠在系统配置之上。macOS 企业配置可通过 MDM 下发 TOML。\n- **`0.79.0`**：会话可以程序化拉起或给其他对话发消息；app-server 增加 `thread/rollback`。\n\n`features list` 在这一段大约 16–18 项，多数仍是 experimental / beta。不要把 `enable_elevated_windows_sandbox` 这种短暂存在的名字写进仓库。\n\n## 升级时\n\n从 `0.69` 跨到 `0.79`，命令表不用改。优先核对三件事：仓库里有没有 `.codex/config.toml`、Skill 是否按会话只加载一次、`codex exec resume` 是否已经能直接跟 `--json` / `--model`。\n";
