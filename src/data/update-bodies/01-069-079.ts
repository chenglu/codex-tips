export const body = "## 0.79.0\n\n- 新增：`exec resume` 和 `exec review` 支持模型、JSON、sandbox 等全局参数，可直接传入 `--json` 和 `--model`。\n- 新增：支持通过程序接口启动会话、向其他会话发送消息。\n- 新增：app-server 提供 `thread/rollback`。\n\n## 0.78.0\n\n- 新增：通过 `Ctrl+G` 在 `$VISUAL` 或 `$EDITOR` 中编辑提示。\n- 新增：加载项目层 `.codex/config.toml`，通过 `project_root_markers` 确定项目边界。\n- 新增：macOS 企业配置支持通过 MDM 下发 TOML。\n\n## 0.77.0\n\n- 移除：`rmcp_client` 特性开关。Streamable HTTP MCP 的 OAuth 认证不再依赖该开关。\n\n## 0.76.0\n\n- 新增：UNIX 系统加载 `/etc/codex/requirements.toml`。\n- 新增：TUI `/ps` 命令。\n\n## 0.74.0\n\n- 新增：`/experimental` 入口，用于管理实验性功能。\n- 改进：`unified_exec` 进入 beta。\n- 变更：Windows sandbox 相关特性名称调整。\n- 变更：默认模型调整为 `gpt-5.2-codex`。\n\n## 0.72.0\n\n- 新增：`enable_elevated_windows_sandbox` 实验性特性开关。\n\n## 0.71.0\n\n- 变更：默认模型调整为 GPT-5.2。\n\n## 0.69.0\n\n- 新增：显式选择 Skill 时，将 `SKILL.md` 加载到当前会话；每个技能按会话加载一次。\n";
