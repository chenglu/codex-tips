export const body = "这一段命令表只加了一个动词：`fork`。默认 API 模型换成 `gpt-5.2-codex`。协作和连接器开始从协议走进 TUI，但多数仍标 experimental。\n\n## 命令与参数\n\n`0.80.0` 还是 13 个顶层命令。`0.81.0` 增加：\n\n```\ncodex fork [OPTIONS] [SESSION_ID] [PROMPT]\n```\n\n省略 session id 时用 `--last` 分叉最近一次会话。同一版根命令出现 `--no-alt-screen`（Zellij 一类终端的备用屏问题），`resume` 也接了这颗参数。\n\n`0.88.0`：`cloud list` 出现 `--json` / `--limit` / `--cursor` / `--env`；`exec resume` 增加 `--all`。\n\n`features list` 在 `0.80.0` 砍掉 `parallel`、`skills`、`view_image_tool`、`warnings` 这些旧名，并不代表功能没了，只是不再以这些键出现。`0.89.0` 去掉 `tui2`。`0.81.0` 增加 `collab` / `steer`；`0.84.0` 用 `child_agents_md` 替换 `hierarchical_agents`。\n\n## 主要功能变化\n\n- **`0.80.0`**：协议和 app-server 支持把会话 fork 成新线程；TUI 增加 `/elevate-sandbox`。\n- **`0.81.0`**：无浏览器环境会自动改走 device-code 登录；Linux 沙箱可以把路径只读挂进去。`codex mcp-server` 返回的 `codex` 工具带上 `threadId`，才能接 `codex-reply`。\n- **`0.86.0`**：Skill 元数据可写在 `SKILL.toml`（名称、描述、图标、默认提示）。\n- **`0.89.0`**：`/permissions` 出现，`/approvals` 仍兼容。`/skill` 用来开关单个技能。app-server 增加 `thread/read`，并能按 cwd 算有效配置。\n- **`0.90.0`**：网络沙箱代理；Connectors 第一期；TUI 协作模式进 beta，Plan / Coding 分得更清楚。\n- **`0.91.0`**：子代理上限收到 6 个。\n\n## 升级时\n\n脚本和文档如果还在写「开一个新 worktree 再 `codex resume`」，从 `0.81` 起可以先试 `codex fork --last`。无头 CI 登录不要再假设一定有浏览器回调。`tui2` 这颗特性名在 `0.89` 消失，不要再写进配置。\n";
