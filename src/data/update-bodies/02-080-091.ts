export const body = "## 0.91.0\n\n- 变更：子代理数量上限调整为 6 个。\n\n## 0.90.0\n\n- 新增：网络沙箱代理和首批 Connectors 支持。\n- 改进：TUI 协作模式进入 beta，区分 Plan 与 Coding 模式。\n\n## 0.89.0\n\n- 新增：`/permissions`，继续兼容 `/approvals`。\n- 新增：通过 `/skill` 启用或关闭单个技能。\n- 新增：app-server 提供 `thread/read`，并支持按工作目录计算有效配置。\n- 移除：`tui2` 特性开关。\n\n## 0.88.0\n\n- 新增：`cloud list` 支持 `--json`、`--limit`、`--cursor` 和 `--env`。\n- 新增：`exec resume` 支持 `--all`。\n\n## 0.86.0\n\n- 新增：通过 `SKILL.toml` 配置技能名称、描述、图标和默认提示。\n\n## 0.84.0\n\n- 变更：`hierarchical_agents` 更名为 `child_agents_md`。\n\n## 0.81.0\n\n- 新增：`codex fork`，用于从已有会话创建分支。`--last` 可选择最近一次会话。\n\n```\ncodex fork [OPTIONS] [SESSION_ID] [PROMPT]\n```\n\n- 新增：根命令和 `resume` 支持 `--no-alt-screen`，改善 Zellij 等终端的兼容性。\n- 改进：无浏览器环境自动使用 device-code 登录。\n- 改进：Linux 沙箱支持只读挂载路径。\n- 改进：`codex mcp-server` 的 `codex` 工具返回 `threadId`，供 `codex-reply` 继续会话。\n- 新增：`collab` 和 `steer` 特性开关。\n\n## 0.80.0\n\n- 新增：协议与 app-server 支持将会话 fork 为新线程。\n- 新增：TUI `/elevate-sandbox`。\n- 变更：默认 API 模型为 `gpt-5.2-codex`。\n- 变更：`parallel`、`skills`、`view_image_tool` 和 `warnings` 不再作为独立特性键列出。\n";
